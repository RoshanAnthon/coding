/* this is linked list*/
#include <iostream>
using namespace std;

int main()
{
    list<int> l;

    /* all functions are same as vector, everything!!!
    extra are as below */
    ls.push_front(1);
    ls.emplace_front(0);

    /* advantage --- read linked list advantage over array/vector */

}